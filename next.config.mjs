/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['unsplash.com'], // Add any other domains you want to use for images
      },
};

export default nextConfig;
